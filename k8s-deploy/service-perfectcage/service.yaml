apiVersion: v1
kind: Service
metadata:
  name: service-perfectcage
  namespace: cage
spec:
  selector:
    app: service-perfectcage
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
