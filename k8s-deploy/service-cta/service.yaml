apiVersion: v1
kind: Service
metadata:
  name: service-cta
  namespace: cage
spec:
  selector:
    app: service-cta
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
