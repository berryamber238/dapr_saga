apiVersion: v1
kind: Service
metadata:
  name: business-coordinator
  namespace: cage
spec:
  selector:
    app: business-coordinator
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
