apiVersion: v1
kind: Service
metadata:
  name: service-notification
  namespace: cage
spec:
  selector:
    app: service-notification
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
